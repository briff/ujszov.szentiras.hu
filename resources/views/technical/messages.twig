{% extends 'adminLayout' %}

{% block content %}
    <h3>Hibásnak tűnő sorok</h3>
    <table class="table">
        {% for message in messages %}
            <tr><td>{{ message.line }}</td><td>{{ message.message }}</td></tr>
        {% endfor %}
    </table>
{% endblock %}